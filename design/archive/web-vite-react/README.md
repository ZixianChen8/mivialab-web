# Archived: Vite + React + Tailwind scaffold

Previous production experiment (pre–Main port). Replaced by the root [`web/`](../../../web/) Next.js app that implements [`design/main/`](../../main/).

Kept for reference only — do not deploy.
