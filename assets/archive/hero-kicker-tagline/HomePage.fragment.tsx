/**
 * Hero copy removed from live HomePage on 2026-07-26.
 * Paste back inside `.hero-dock__intro` / `.hero-dock__support` as needed.
 */

export const HERO_KICKER = (
  <p className="hero-kicker">Ottawa–Toronto studio</p>
);

export const HERO_BRAND_TAGLINE = (
  <p className="brand-tagline">Custom sites, cared for end to end.</p>
);
